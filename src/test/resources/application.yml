cloudinary:
  api_key: 574976897654358
  api_secret: TlurERJM-a8VjMtsm8CzdTLM7Dg
  cloud_name: dr3m6za90
server:
  port: 8080
spring:
  #This is the default in-memory credentials, we can change them if we want
  #    datasource:
  #        url:
  #            jdbc:h2:mem:testdb
  #        driver-class-name:
  #            org.h2.Driver
  #        username: sa
  #        password: ''
  jpa:
    #For H2 Database
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 5MB
  mvc:
    hiddenmethod:
      filter:
        enabled: true
